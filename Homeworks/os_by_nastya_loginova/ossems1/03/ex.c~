#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main() {

uid_t uid; 
gid_t gid;

uid = getuid ();
gid = getgid ();

printf ("\n User ID %d\n", (int)uid);

printf ("\n Group ID %d\n", (int)gid);

}
