#include <sys/types.h>
#include <unistd.h>


int main()
{
    uid_t getuid(void);
    gid_t getgid(void);

  printf("My uid = %d, my uid = %d, result = %d\n", (int)uid, (int)uid, a);

return 0;
        
}
